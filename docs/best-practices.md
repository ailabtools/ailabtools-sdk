# SDK 最佳实践与性能优化

## 请求与超时
- 建议为请求设置超时（Node: `timeoutMs`，Python: `timeout`）
- 对上传大文件的接口，建议适当提高超时（60s+）

## 重试策略
- 对网络错误或 5xx，可采用指数退避（1s, 2s, 4s, 8s）
- 避免对参数错误/鉴权错误做重试

## 并发与限流
- 控制并发请求数（建议 3~10）
- 如果遇到 429，降低并发并增加退避

## 异步任务
- 异步结果轮询间隔建议 5s
- 结果 24 小时内有效，请及时下载

